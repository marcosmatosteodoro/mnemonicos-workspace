# Calibração de estimativas

> Par estimado × realizado por demanda fechada — memória do projeto (não do plugin).
> O `estimator` lê este arquivo antes de estimar (`estimate-contract.md` §4); com menos
> de 3 demandas fechadas, declara `sem base histórica` e não finge calibração.

- 2026-09-07 · producao-material — PWA no mnemonicos-frontend (PLAN-013) · previsto: ~4 waves/~7 tasks · 18–44h · realizado: 6 waves/6 tasks (6 de 6 TASKs com marca) · parede 5h02min · trabalho 4h31min · desvio: muito abaixo da faixa (trabalho ~4–10× menor que o piso estimado) — a estimativa original não previu a rota real de execução (subagents de gate rodando em paralelo dentro da wave, sem espera humana entre rodadas) nem que 3 das 6 tasks (ACs de ciclo de vida do SW) fechariam por `pendente_handoff` em vez do handoff manual completo ter sido de fato caminhado nesta sessão; primeira calibração real do projeto para capacidade de service worker/PWA.
